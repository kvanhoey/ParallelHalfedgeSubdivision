#include "mesh.h"

void
Mesh_Subdiv_Loop_GPU::refine_halfedges()
{
	for (uint d = 0 ; d < D; ++d)
	{
		assert(false) ;
	}
}

void
Mesh_Subdiv_Loop_GPU::refine_vertices()
{
	for (uint d = 0 ; d < D; ++d)
	{
		assert(false) ;
	}
}
